export const metadata = {
  title: "Analisis Daun AI",
  description: "Deteksi penyakit daun dengan AI",
};

export default function RootLayout({ children }) {
  return (
    <html lang="id">
      <body>
        {children}
      </body>
    </html>
  );
    } 
